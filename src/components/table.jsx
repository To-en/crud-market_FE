// Ingredient category filter bar — flat list, no nesting
// Props: categories (string[]), selected, onSelect
export function CategoryBar({ categories, selected, onSelect }) {
  // TODO: render a horizontal list of category pills
  // TODO: highlight the selected one
  // TODO: call onSelect(category) on click
}
