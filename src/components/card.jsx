// Generic card shell — header + body slot
// Props: title, children, action (optional button in header)
export function Card({ title, children, action }) {
  // TODO: render <div className="card">
  // TODO: render <div className="card-header"> with title + optional action
  // TODO: render children inside card body
}
