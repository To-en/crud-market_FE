// Controlled checkbox with label
// Props: checked, onChange, label
export function Checkbox({ checked, onChange, label }) {
  // TODO: render <input type="checkbox"> + <label>
  // TODO: wire checked + onChange as controlled component
}
