// Top navigation bar — shows app title + links to each page
// Props: none (reads route from React Router, links via <Link>)
export function Navbar() {
  // TODO: import { Link, useLocation } from "react-router-dom"
  // TODO: render links: /market  /cart  /order-history  /ingredients
  // TODO: highlight active link using useLocation().pathname
}
